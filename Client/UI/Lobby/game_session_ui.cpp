#include "game_session_ui.h"

Game_session_ui::Game_session_ui(QString game_name, int max_players, int current_players, int count_bots, int min_bet, int starting_chips)
    :game_name(game_name), max_players(max_players), current_players(current_players), count_bots(count_bots), min_bet(min_bet), starting_chips(starting_chips)
{}
